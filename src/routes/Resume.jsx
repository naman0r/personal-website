import React from "react";
import "../App.css";
import gsap from "gsap";
import TopNav from "../TopNav";

/* can this just be a page through which they can see my creative resume, adn then also have a option to download it */

const Resume = () => {
  return (
    <div>
      <TopNav />
      <div>
        <p>
          {" "}
          Navigation bar doesnt show as of now. this is a work in progress!
        </p>
      </div>
    </div>
  );
};

export default Resume;
